package iutinfo.lp.devmob.orientacourse

object currentCourse {
    var currentMode="";
    var currentCourse = "";
    var startTime="";
    var finishTime="";
    var currentLatPoints= listOf<Double>();
    var currentLongPoints= listOf<Double>();
}